# Supabase dashboard → Project Settings → API
# Copy this file to .env.local, then paste the project URL and publishable/anon key.
# Never expose the service_role key in the client.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
